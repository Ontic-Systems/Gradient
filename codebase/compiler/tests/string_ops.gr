mod string_ops

fn greet(name: String) -> !{Heap} String:
    ret "Hello, " + name + "!"

fn main() -> !{IO, Heap} ():
    print(greet("World"))
    print(greet("Gradient"))
    let msg: String = "foo" + "bar" + "baz"
    print(msg)
