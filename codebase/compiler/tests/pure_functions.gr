mod pure_functions

fn double(x: Int) -> Int:
    ret x * 2

fn add_one(x: Int) -> Int:
    ret x + 1

fn apply_twice(x: Int) -> Int:
    ret double(double(x))

fn main() -> !{IO} ():
    print_int(double(5))
    print_int(add_one(double(3)))
    print_int(apply_twice(3))
