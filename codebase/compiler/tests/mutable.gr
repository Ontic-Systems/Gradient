mod mutable

fn sum_to(n: Int) -> Int:
    let mut total: Int = 0
    let mut i: Int = 1
    while i <= n:
        total = total + i
        i = i + 1
    ret total

fn main() -> !{IO} ():
    print_int(sum_to(10))
