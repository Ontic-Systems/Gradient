# Gradient Next Steps

This file tracks what to work on next for the Gradient programming language.

## Current State (April 2025)

### Recently Completed

1. **Comptime Foundation** (Phase 1 - DONE)
   - Parser: `comptime` keyword, Param.comptime flag, `TypeExpr::Type`
   - Type System: `Ty::Type`, `Binding` struct with comptime tracking
   - Evaluator: New comptime module with expression evaluator
   - Tests: 895 tests passing
   - Commit: 97323a8

### Priority Order

1. **Comptime Phase 2** - IN PROGRESS
   - [ ] Connect evaluator to type checker for comptime function calls
   - [ ] Add comptime param validation in function calls
   - [ ] Implement type instantiation from comptime values
   - [ ] Add tests for comptime generic instantiation

2. **Package Registry** - AFTER COMPTIME
   - Remote package fetching via GitHub
   - Semver resolution
   - Integration with LSP/import system

3. **WASM Target Polish** - PARALLEL
   - Runtime linking (currently stubbed)
   - WASI integration for I/O
   - Remove wasm_e2e test ignores

4. **SMT Verification** - LATER
   - Z3 integration
   - Precondition/postcondition checking
   - Effect verification

5. **Self-Hosting** - DISTANT
   - Requires: comptime + package system mature
   - Rewrite compiler in Gradient

### Technical Debt

- 4 WASM E2E tests ignored (feature flag issue)
- 2 compiler warnings about unused code (expected for comptime)

### Notes

The comptime foundation is solid. The next step is integration with
type checking so that functions like `Vector(comptime T: type, N: usize)`
can be instantiated with types at compile time.
